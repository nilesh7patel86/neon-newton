package com.neon.newton.plugin.calendar;

import org.pf4j.Plugin;
import org.pf4j.PluginWrapper;

public class CalendarPlugin extends Plugin {
    public CalendarPlugin(PluginWrapper wrapper) {
        super(wrapper);
    }
}
