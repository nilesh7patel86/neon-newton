package com.neon.newton.plugin.clock;

import org.pf4j.Plugin;
import org.pf4j.PluginWrapper;

public class ClockPlugin extends Plugin {
    public ClockPlugin(PluginWrapper wrapper) {
        super(wrapper);
    }
}
