package com.neon.newton.plugin.currency;

import org.pf4j.Plugin;
import org.pf4j.PluginWrapper;

public class CurrencyPlugin extends Plugin {
    public CurrencyPlugin(PluginWrapper wrapper) {
        super(wrapper);
    }
}
